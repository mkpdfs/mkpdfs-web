---
title: Errors
description: Error codes, status codes, and retry guidance.
---

All API errors return a JSON body with `code` and `message` fields; HTTP 402 means insufficient credits, 429 means rate-limited. _(Full content coming.)_
